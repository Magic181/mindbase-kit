# 环境配置

## 一、开发环境配置

### 操作系统

| 系统 | 版本 | 支持状态 |
|------|------|----------|
| Windows | 11（推荐） | 完全支持 |
| Windows | 10 | 支持 |
| macOS | 13+ | 支持 |
| Ubuntu | 22.04+ | 支持 |

### 开发工具

| 工具 | 版本 | 说明 |
|------|------|------|
| Python | 3.12+ | 性能更优，长期支持 |
| Node.js | 22 LTS | Vue3 + Vite 最佳兼容 |
| pnpm | 10+ | 快速、节省磁盘 |
| Docker Desktop | 最新 | 容器化服务 |
| Git | 最新 | 版本控制 |
| VS Code / Cursor | 最新 | 开发IDE |

### Python 环境配置

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# macOS/Linux
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

### Node.js 环境配置

```bash
# 安装 pnpm
npm install -g pnpm@10

# 安装依赖
cd frontend
pnpm install
```

### Docker 配置

```bash
# 启动基础服务
docker compose -f docker-compose.dev.yml up -d

# 服务列表
# - MySQL: 3306
# - Redis: 6379
```

---

## 二、生产环境配置

### 服务器配置

| 项目 | 最低配置 | 推荐配置 |
|------|----------|----------|
| 操作系统 | Ubuntu 24.04 LTS | Ubuntu 24.04 LTS |
| CPU | 2核 | 4核+ |
| 内存 | 4GB | 8GB+ |
| 磁盘 | 20GB | 100GB+ SSD |
| 网络 | 10Mbps | 100Mbps+ |

### 软件环境

| 软件 | 版本 | 说明 |
|------|------|------|
| Docker | 24+ | 容器运行时 |
| Docker Compose | 2.20+ | 服务编排 |
| Nginx | 1.24+ | 反向代理 |
| Let's Encrypt | - | SSL证书 |

### 服务端口规划

| 服务 | 端口 | 说明 |
|------|------|------|
| Nginx | 80/443 | HTTP/HTTPS |
| Frontend | 3000 | Vue3 前端 |
| Backend | 8000 | Django API |
| MySQL | 3306 | 关系数据库 |
| Redis | 6379 | 缓存/队列 |
| Milvus | 19530 | 向量数据库 |
| MinIO | 9000 | 对象存储 |

---

## 三、桌面版配置

### 开发环境

| 工具 | 版本 | 说明 |
|------|------|------|
| Node.js | 22 LTS | Electron 构建 |
| Python | 3.12+ | 内嵌后端 |
| Electron | 28+ | 桌面框架 |
| electron-builder | 24+ | 打包工具 |

### 打包环境

| 目标平台 | 打包工具 | 产物格式 |
|----------|----------|----------|
| Windows | NSIS | .exe |
| macOS | DMG | .dmg |
| Linux | AppImage | .AppImage |

### 桌面版数据存储

```
# 用户数据目录
%APPDATA%/AI Notebook/        # Windows
~/Library/Application Support/AI Notebook/  # macOS
~/.config/AI Notebook/        # Linux

# 数据库
ai_notebook.db                # SQLite

# 文件存储
files/                        # 本地文件
```

---

## 四、环境变量配置

### 开发环境 (.env)

项目根目录使用单一 `.env` 文件（参考 `.env.example`）：

```env
# Django
DJANGO_SECRET_KEY=dev-secret-key-change-in-production
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database: USE_SQLITE=true 使用 SQLite；false 时使用 MySQL
USE_SQLITE=true
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=ai_notebook
MYSQL_USER=ai_notebook
MYSQL_PASSWORD=dev-password

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_ACCESS_TOKEN_MINUTES=5
JWT_REFRESH_TOKEN_DAYS=7

# Celery
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_TASK_ALWAYS_EAGER=true
MAX_UPLOAD_SIZE_MB=20

# DeepSeek (OpenAI-compatible)
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-v4-flash
DEEPSEEK_API_KEY=
DEEPSEEK_TIMEOUT_SECONDS=60
DEEPSEEK_MAX_RETRIES=2
DEEPSEEK_RETRY_BACKOFF_SECONDS=0.2

# Tavily web search (可选)
TAVILY_API_KEY=
TAVILY_MAX_RESULTS=5
TAVILY_SEARCH_DEPTH=basic
TAVILY_TIMEOUT_SECONDS=20
TAVILY_MAX_RETRIES=1
TAVILY_RETRY_BACKOFF_SECONDS=0.2

# RAG
RAG_TOP_K=5
RAG_MAX_CONTEXT_CHARS=8000
```

### 开发环境 (.env.development) — 历史参考

### 生产环境 (.env.production)

```env
# Django
DJANGO_SECRET_KEY=your-production-secret-key
DJANGO_DEBUG=false
DJANGO_ALLOWED_HOSTS=your-domain.com

# Database
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_DATABASE=ai_notebook
MYSQL_USER=ai_notebook
MYSQL_PASSWORD=your-secure-password

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# AI
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-v4-flash
DEEPSEEK_API_KEY=your-deepseek-key
TAVILY_API_KEY=your-tavily-key
```

### 桌面版配置

```env
# 桌面版使用本地存储，无需配置数据库
DJANGO_DEBUG=false
DATABASE_URL=sqlite:///ai_notebook.db
STORAGE_PATH=./files
```

---

## 五、配置验证

### 开发环境验证

```bash
# 检查 Python 版本
python --version  # 应显示 3.12+

# 检查 Node.js 版本
node --version    # 应显示 v22.x.x

# 检查 pnpm 版本
pnpm --version    # 应显示 10.x.x

# 检查 Docker 状态
docker compose ps
```

### 生产环境验证

```bash
# 检查服务状态
docker compose ps

# 检查 Nginx 配置
sudo nginx -t

# 检查 SSL 证书
sudo certbot certificates
```

---

## 六、常见配置问题

### Q: Python 虚拟环境激活失败

```bash
# Windows
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
venv\Scripts\Activate.ps1

# macOS/Linux
source venv/bin/activate
```

### Q: pnpm 安装超时

```bash
# 使用国内镜像
pnpm config set registry https://registry.npmmirror.com
```

### Q: Docker 服务启动失败

```bash
# 检查端口占用
netstat -tulpn | grep :3306

# 重启 Docker Desktop
```

### Q: 登录后数据为空

确认当前后端连接的是 SQLite 还是 MySQL。`USE_SQLITE=true` 会使用 `backend/db.sqlite3`，与 MySQL 中的账号和数据互不共享。

### Q: AI 能回答问候，但资料问题提示资料不足

先在 Notebook 中上传 TXT/MD/PDF/DOCX 文档并等待解析完成。不开启联网搜索时，资料型问题只基于已上传文档回答。

### Q: 联网搜索没有结果

确认 `.env` 中已配置 `TAVILY_API_KEY`，并重启 Django 后端。联网搜索只在 Chat 页面勾选“联网搜索”后启用。
